$schema: https://json-schema.org/draft/2020-12/schema
$id: yss:doc.changelog
title: Changelog
description: Releases newest first. Change types follow keep-a-changelog; status from the `release_status` vocabulary.
type: object
required: [kind, title, releases]
additionalProperties: false
properties:
  kind: {const: changelog}
  releases:
    type: array
    items:
      type: object
      allOf: [{$ref: "#/$defs/item_base"}]
      required: [version, changes]
      properties:
        version: {type: string}
        date: {type: string, format: date}
        status: {type: string, x-vocab: release_status, default: released}
        summary: {type: string, x-limit: summary}
        changes:
          type: array
          items:
            type: object
            required: [type, text]
            properties:
              type: {type: string, enum: [added, changed, fixed, removed, deprecated, security]}
              text: {type: string, x-limit: summary}
              refs: {type: array, items: {type: string}}
              visibility: {$ref: "#/$defs/visibility"}
            additionalProperties: false
      unevaluatedProperties: false
